<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SORRY RI</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 0;
      background: linear-gradient(120deg, #ff7c7c, #ffb3b3);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
    }

    .container {
      position: relative;
      text-align: center;
      background-color: rgba(0, 0, 0, 0.6);
      padding: 50px;
      border-radius: 15px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.2);
      z-index: 1;
    }

    h1 {
      color: white;
      font-size: 4rem;
      margin-bottom: 20px;
      animation: slideIn 2s ease-in-out;
    }

    p {
      color: white;
      font-size: 1.2rem;
      line-height: 1.8;
      margin: 20px 0;
      animation: fadeIn 2s ease-in-out;
      animation-delay: 1s;
    }

    .heart {
      font-size: 100px;
      color: red;
      animation: pulse 2s infinite;
    }

    .apology-btn {
      background-color: #ff7c7c;
      color: white;
      font-size: 1.2rem;
      border: none;
      padding: 15px 30px;
      border-radius: 8px;
      cursor: pointer;
      transition: transform 0.3s ease-in-out;
    }

    .apology-btn:hover {
      background-color: #ffb3b3;
      transform: scale(1.1);
    }

    .circle, .triangle, .rectangle {
      position: absolute;
      background-color: rgba(255, 255, 255, 0.3);
      animation: moveShape 4s ease-in-out infinite;
    }

    .circle {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      top: 20%;
      left: 10%;
      animation-delay: 0s;
    }

    .triangle {
      width: 0;
      height: 0;
      border-left: 50px solid transparent;
      border-right: 50px solid transparent;
      border-bottom: 100px solid rgba(255, 255, 255, 0.3);
      top: 50%;
      left: 70%;
      animation-delay: 2s;
    }

    .rectangle {
      width: 150px;
      height: 60px;
      top: 80%;
      left: 40%;
      animation-delay: 1s;
    }

    @keyframes slideIn {
      0% {
        transform: translateY(-50px);
        opacity: 0;
      }
      100% {
        transform: translateY(0);
        opacity: 1;
      }
    }

    @keyframes fadeIn {
      0% {
        opacity: 0;
      }
      100% {
        opacity: 1;
      }
    }

    @keyframes pulse {
      0% {
        transform: scale(1);
      }
      50% {
        transform: scale(1.2);
      }
      100% {
        transform: scale(1);
      }
    }

    @keyframes moveShape {
      0% {
        transform: translateX(0) translateY(0);
      }
      50% {
        transform: translateX(200px) translateY(200px);
      }
      100% {
        transform: translateX(0) translateY(0);
      }
    }
  </style>
</head>
<body>
  <div class="circle"></div>
  <div class="triangle"></div>
  <div class="rectangle"></div>

  <div class="container">
    <h1>SORRY RI ANU 💖</h1>
    <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SORRY RI</title>
  <style>
    body {
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 0;
      background: linear-gradient(120deg, #ff7c7c, #ffb3b3);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      overflow: hidden;
    }

    .container {
      position: relative;
      text-align: center;
      background-color: rgba(0, 0, 0, 0.6);
      padding: 50px;
      border-radius: 15px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.2);
      z-index: 1;
    }

    h1 {
      color: white;
      font-size: 4rem;
      margin-bottom: 20px;
      animation: slideIn 2s ease-in-out;
    }

    p {
      color: white;
      font-size: 1.2rem;
      line-height: 1.8;
      margin: 20px 0;
      animation: fadeIn 2s ease-in-out;
      animation-delay: 1s;
    }

    .heart {
      font-size: 100px;
      color: red;
      animation: pulse 2s infinite;
    }

    .apology-btn {
      background-color: #ff7c7c;
      color: white;
      font-size: 1.2rem;
      border: none;
      padding: 15px 30px;
      border-radius: 8px;
      cursor: pointer;
      transition: transform 0.3s ease-in-out;
    }

    .apology-btn:hover {
      background-color: #ffb3b3;
      transform: scale(1.1);
    }

    .circle, .triangle, .rectangle {
      position: absolute;
      background-color: rgba(255, 255, 255, 0.3);
      animation: moveShape 4s ease-in-out infinite;
    }

    .circle {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      top: 20%;
      left: 10%;
      animation-delay: 0s;
    }

    .triangle {
      width: 0;
      height: 0;
      border-left: 50px solid transparent;
      border-right: 50px solid transparent;
      border-bottom: 100px solid rgba(255, 255, 255, 0.3);
      top: 50%;
      left: 70%;
      animation-delay: 2s;
    }

    .rectangle {
      width: 150px;
      height: 60px;
      top: 80%;
      left: 40%;
      animation-delay: 1s;
    }

    @keyframes slideIn {
      0% {
        transform: translateY(-50px);
        opacity: 0;
      }
      100% {
        transform: translateY(0);
        opacity: 1;
      }
    }

    @keyframes fadeIn {
      0% {
        opacity: 0;
      }
      100% {
        opacity: 1;
      }
    }

    @keyframes pulse {
      0% {
        transform: scale(1);
      }
      50% {
        transform: scale(1.2);
      }
      100% {
        transform: scale(1);
      }
    }

    @keyframes moveShape {
      0% {
        transform: translateX(0) translateY(0);
      }
      50% {
        transform: translateX(200px) translateY(200px);
      }
      100% {
        transform: translateX(0) translateY(0);
      }
    }
  </style>
</head>
<body>
  <div class="circle"></div>
  <div class="triangle"></div>
  <div class="rectangle"></div>

  <div class="container">
    <h1>sorry ri ANU</h1>
    <p>Nan taph madidino ilvo gotilla but without reason block madidakke hurt aytu.aah sagar na block madodakke hesitate madidri nanna isht easy agi without any second thought block madidri.naan avn ginta keel aghbitna? reason adru heli nim pov nalli henadru taph madidre im really sorry .</p>
    <div class="heart">💔</div>
    <button class="apology-btn" onclick="showMessage()">I Accept Your Apology</button>
  </div>

  <script>
    function showMessage() {
      alert("HOPE UNBLOCK MADI PROPER REASON AND CLARIFICATION KODTIRA ANTA .");
    }
  </script>
</body>
</html>
    <div class="heart">💔</div>
    <button class="apology-btn" onclick="showMessage()">I Accept Your Apology</button>
  </div>

  <script>
    function showMessage() {
      alert("I HOPE EEGA UNBLOCK MADI CLARITY KODTIRA HENH AYTU ANYTA ANKONIDDINI");
    }
  </script>
</body>
</html>
